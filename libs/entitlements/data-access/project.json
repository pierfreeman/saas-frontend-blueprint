{
  "name": "entitlements-data-access",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/entitlements/data-access/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["type:data-access", "scope:entitlements"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run --config libs/entitlements/data-access/vitest.config.ts"
      },
      "configurations": {
        "coverage": {
          "command": "npx vitest run --config libs/entitlements/data-access/vitest.config.ts --coverage"
        }
      }
    }
  }
}
